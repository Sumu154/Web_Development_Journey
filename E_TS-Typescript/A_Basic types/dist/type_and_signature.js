"use strict";
const userDetails = (id, user) => {
    console.log(`user id is ${id}, name is ${user.name} and age is ${user.age} `);
};
// function signature
let myFunc; // -> void hocce return type
let add;
// not const add X
add = (a, b) => {
    return a + b;
};
